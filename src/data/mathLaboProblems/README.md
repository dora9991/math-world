# mathLaboProblems/

数学ラボ2（`~/Documents/Claude/Projects/数学教材/数学ラボ2/src/data/grade1/*.js`）の
問題生成ファイルを**そのままコピー**したもの（2026-09-14）。

- `c1_seisu.js` 〜 `c7_data.js` の7ファイルは中1の全7章ぶん。
- 各ファイルは外部依存なし（`build(r)` の `r` だけを引数で受け取る自己完結形）
  だったため、無改造でコピーできた。
- 「共有するのはデータだけ・エンジンもUIも共有しない」という8/11の方針どおり、
  コードごとコピーする形にした（`math-problems/`への単一ソース化は依然未着手）。
  **今後、数学ラボ2側でこれらのファイルが更新されても、math-world側は自動では
  追従しない。** 内容を合わせたくなったら再コピーが必要（#todo 自動化するなら
  単一ソース化を先にやる）。

呼び出し側は `src/engine/mathLaboProblems.js`。
